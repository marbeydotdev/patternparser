{
  "name": "patternparser",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc"
  },
  "keywords": ["cli", "parsing", "parser", "command", "typescript"],
  "author": "Zavid van den Worm",
  "license": "MIT",
  "description": "A command parser with an incredibly simple setup.",
  "dependencies": {
    "validator": "^13.15.0"
  },
  "devDependencies": {
    "@types/validator": "^13.12.3"
  }
}
